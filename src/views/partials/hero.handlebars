<section class="hero">
  <div class="hero-bg">
    <a class="attribution" href="{{ labels.hero.cover.attribution.link }}" target="_blank">
      {{{ labels.hero.cover.attribution.text }}}
    </a>
  </div>
  <h1>
    <span>{{{ labels.hero.title.[0] }}}</span>
    <span>{{{ labels.hero.title.[1] }}}</span>
  </h1>
  <div class="actions">
    <a href="{{ getUrl 'course' }}" class="action dark">{{ labels.hero.links.course }}</a>
    <a href="{{ getUrl 'lessons' }}" class="action dark">{{ labels.hero.links.lessons }}</a>
  </div>
</section>